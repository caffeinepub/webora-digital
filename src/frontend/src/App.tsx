/**
 * App.tsx — Not directly used as entry point.
 * The app uses RouterProvider in main.tsx with the router defined in router.tsx.
 * Layout, pages, and navigation are wired through the route tree.
 */
export default function App() {
  return null;
}
